# Modding Help

Modding is the largely the same as Psych Engine modding, as this mod is Psych based.

## Particularies

`dialogue.json` and `after.json` files will be automatically played with `default_dialogue.ogg` music playing. If you want custom music, for now just name the dialogue file differently and play using Lua.
